# uncomment if you plan to run the integration setup manually against kube-ci
unset AWS_REGION
unset AWS_ACCESS_KEY_ID
unset AWS_SECRET_ACCESS_KEY
source_env ../cailleach/.envrc

# allow local .envrc overrides
[[ -f .envrc.local ]] && source_env .envrc.local
