cargohold:
  host: 0.0.0.0
  port: 8084

aws:
  # TODO: Check AWS loadCredentialsFromEnv
  keyId: foo
  secretKey: bar
  s3Bucket: local-cargohold
  cfDomain: local-cargohold
  cfKeyPairId: local-cargohold
  cfPrivateKey: config/cf.pem

settings:
  maxTotalBytes: 27262976

# TODO: Set this to true _ONLY_ for testing purposes
#       as this causes SSL certicates not to be verified
skipVerifySSL: true
