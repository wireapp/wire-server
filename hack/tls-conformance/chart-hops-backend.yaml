apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tr-chart-hello
  namespace: joe-test
  annotations:
    external-dns.alpha.kubernetes.io/controller: ignored
spec:
  parentRefs:
    - name: tr-chart
  rules:
    - backendRefs:
        - name: tr-hello
          port: 8080
