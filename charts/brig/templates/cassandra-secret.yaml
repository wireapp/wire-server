{{- if  (include "useCassandraCA" .Values.config) }}
apiVersion: v1
kind: Secret
metadata:
  name: brig-cassandra
  labels:
    app: brig
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  ca.pem: {{ .Values.config.cassandra.tlsCert | quote }}
{{- end }}
