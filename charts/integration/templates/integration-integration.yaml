apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-integration-integration"
  annotations:
    "helm.sh/hook": test
  labels:
    app: integration-integration
    release: {{ .Release.Name }}
spec:
  volumes:
    - name: "integration-config"
      configMap:
        name: "integration-config"

    - name: "galley-config"
      configMap:
        name: "galley"

    - name: "galley-secrets"
      secret:
        secretName: "galley"

    - name: "brig-config"
      configMap:
        name: "brig"

    - name: "brig-secrets"
      configMap:
        name: "brig"

    - name: "turn-servers"
      configMap:
        name: "turn"

  containers:
  - name: integration
    image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
    command: [ "integration", "--config", "/etc/wire/integration/integration.yaml" ]
    volumeMounts:

    - name: "integration-config"
      mountPath: "/etc/wire/integration"

    - name: "galley-config"
      mountPath: "/etc/wire/galley/config"

    - name: "galley-secrets"
      mountPath: "/etc/wire/galley/secrets"

    - name: "brig-config"
      mountPath: "/etc/wire/brig/config"

    - name: "brig-secrets"
      mountPath: "/etc/wire/brig/secrets"

    - name: "turn-servers"
      mountPath: "/etc/wire/brig/turn"

  restartPolicy: Never
