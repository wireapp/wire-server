{{- if  (include "useCassandraCA" .Values.config) }}
apiVersion: v1
kind: Secret
metadata:
  name: spar-cassandra
  labels:
    app: spar
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  ca.pem: {{ .Values.config.cassandra.tlsCa | b64enc | quote }}
{{- end }}
