{{/* Secret for the provided Cassandra TLS CA. */}}
{{- if not (empty .Values.spar.config.cassandra.tlsCa) }}
apiVersion: v1
kind: Secret
metadata:
  name: spar-cassandra
  labels:
    app: spar
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  ca.pem: {{ .Values.spar.config.cassandra.tlsCa | b64enc | quote }}
{{- end }}
