{{/* Secret for the provided Cassandra TLS CA. */}}
{{- if not (empty .Values.brig.config.cassandra.tlsCa) }}
apiVersion: v1
kind: Secret
metadata:
  name: brig-cassandra
  labels:
    app: brig
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  ca.pem: {{ .Values.brig.config.cassandra.tlsCa | b64enc | quote }}
{{- end }}
