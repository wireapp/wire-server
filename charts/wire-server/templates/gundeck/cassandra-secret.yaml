{{/* Secret for the provided Cassandra TLS CA. */}}
{{- if not (empty .Values.gundeck.config.cassandra.tlsCa) }}
apiVersion: v1
kind: Secret
metadata:
  name: gundeck-cassandra
  labels:
    app: brig
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  ca.pem: {{ .Values.gundeck.config.cassandra.tlsCa | b64enc | quote }}
{{- end }}
