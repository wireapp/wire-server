{{/* Secrets for provided Cassandra TLS CAs */}}
{{- $backgroundWorker := index .Values "background-worker" }}
{{- if not (empty $backgroundWorker.config.cassandra.tlsCa) }}
apiVersion: v1
kind: Secret
metadata:
  name: background-worker-cassandra-gundeck
  labels:
    app: background-worker
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  ca.pem: {{ $backgroundWorker.config.cassandra.tlsCa | b64enc | quote }}
{{- end }}
{{- if and $backgroundWorker.config.cassandraBrig (not (empty $backgroundWorker.config.cassandraBrig.tlsCa)) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: background-worker-cassandra-brig
  labels:
    app: background-worker
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  ca.pem: {{ $backgroundWorker.config.cassandraBrig.tlsCa | b64enc | quote }}
{{- end }}
{{- if and $backgroundWorker.config.cassandraGalley (not (empty $backgroundWorker.config.cassandraGalley.tlsCa)) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: background-worker-cassandra-galley
  labels:
    app: background-worker
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  ca.pem: {{ $backgroundWorker.config.cassandraGalley.tlsCa | b64enc | quote }}
{{- end }}
