{{- if (include "useTlsCertBrig" .) }}
apiVersion: v1
kind: Secret
metadata:
  name: brig-cassandra-cert
  labels:
    app: cassandra-migrations
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
type: Opaque
data:
  ca.pem: {{ include "tlsCertBrig" . | b64enc | quote }}
{{- end}}
{{- if (include "useTlsCertGalley" .) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: galley-cassandra-cert
  labels:
    app: cassandra-migrations
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
type: Opaque
data:
  ca.pem: {{ include "tlsCertGalley" . | b64enc | quote }}
{{- end}}
{{- if (include "useTlsCertGundeck" .) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: gundeck-cassandra-cert
  labels:
    app: cassandra-migrations
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
type: Opaque
data:
  ca.pem: {{ include "tlsCertGundeck" . | b64enc | quote }}
{{- end}}
{{- if (include "useTlsCertSpar" .) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: spar-cassandra-cert
  labels:
    app: cassandra-migrations
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
type: Opaque
data:
  ca.pem: {{ include "tlsCertSpar" . | b64enc | quote }}
{{- end}}
