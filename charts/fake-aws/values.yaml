# Default values for fake-aws

# Set enabled: false to not install that subchart.

fake-aws-sqs:
  enabled: true
fake-aws-sns:
  enabled: true
fake-aws-s3:
  enabled: true
fake-aws-s3-legacy-minio:
  enabled: false
fake-aws-ses:
  enabled: false
