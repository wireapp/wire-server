apiVersion: v1
kind: ConfigMap
metadata:
  name: "cargohold-integration"
  annotations:
    "helm.sh/hook": test
data:
  integration.yaml: |
    cargohold:
      host: cargohold
      port: {{ .Values.service.internalPort }}
