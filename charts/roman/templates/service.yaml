apiVersion: v1
kind: Service
metadata:
  name: {{ include "roman.fullname" . }}
  labels:
    {{- include "roman.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "roman.selectorLabels" . | nindent 4 }}
