web: LOG_LEVEL=Info $DIST/cargohold --host=$CARGOHOLD_WEB_HOST --port=$PORT --aws-key-id=$AWS_ACCESS_KEY_ID --aws-secret-key=$AWS_SECRET_ACCESS_KEY --aws-s3-bucket=$AWS_ASSETS_BUCKET --max-total-bytes=$UPLOAD_MAX_BYTES --aws-s3-endpoint=$AWS_S3_ENDPOINT #TODO: Uncomment the remaining command line args if cloudfront is to be used --aws-cloudfront-domain=$AWS_CLOUDFRONT_DOMAIN --aws-cloudfront-keypair-id=$AWS_CLOUDFRONT_KEYPAIR_ID --aws-cloudfront-private-key=$AWS_CLOUDFRONT_PRIVATEKEY
