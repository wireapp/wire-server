---
platform: linux
inputs:
  - name: wire-docs
run:
  path: sh
  args:
  - -exc
  - |
    cd wire-docs
    # hack: FUTUREWORK remove this section
    rm -rf /mnt
    ln -s $PWD /mnt
    cd /mnt
    source $HOME/.poetry/env
    poetry install
    # hack end
    make html push
