Normalize discovered federator hostnames before making outbound HTTP/2 requests by stripping a trailing dot from the SRV target. This prevents wire-server from sending a dotted :authority value to remote federators, which is rejected by Envoy Gateway route matching, while staying consistent with existing TLS hostname validation that already treats dotted and non-dotted hostnames as equivalent.
