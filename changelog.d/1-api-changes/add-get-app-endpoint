Add "get app" endpoint to Brig (`GET /teams/:tid/apps/:id`)

Elastic Search re-indexing requires postgres access now.

You need to do a full re-index of ElasticSearch immediately after the
upgrade.  Between server upgrade and a full re-index, old-style bots
(spawned by services) may identify as be listed as regular users in
search results.
